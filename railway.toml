[build]
builder = "nixpacks"
buildCommand = "npm run build"

[build.nixpacks]
plan = { setup = { pkgs = ["nodejs_20"] } }

[deploy]
startCommand = "npm run start"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
